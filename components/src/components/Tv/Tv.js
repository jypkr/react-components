import * as React from 'react';

import { Button } from '../Button';

import './Tv.css';

const Tv = ((props) => {

    return(
        <div className="tv">
            <Button text='Click to Start' location='#' />
        </div>
    );
})

export default Tv;